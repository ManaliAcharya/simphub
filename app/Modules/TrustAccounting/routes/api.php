<?php

use Illuminate\Support\Facades\Route;

Route::prefix('v1/trust-accounting')->name('trust-accounting.')->group(function (): void {
    // Trust accounting API endpoints will be registered here.
});
